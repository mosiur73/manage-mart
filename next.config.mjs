/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      "assets.adidas.com",
      "images.remotePatterns",
      "i.ibb.co.com",
      "res.cloudinary.com",
      "cdn.dummyjson.com",
    ],
}
};

export default nextConfig;
