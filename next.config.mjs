/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      "assets.adidas.com",
      "images.remotePatterns",
      "i.ibb.co.com"
      
    ],
}
};

export default nextConfig;
